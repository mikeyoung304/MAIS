import { GalleryPageSkeleton } from '@/components/tenant/LoadingSkeletons';

/**
 * Loading state for gallery page (domain-based)
 */
export default function GalleryLoading() {
  return <GalleryPageSkeleton />;
}
