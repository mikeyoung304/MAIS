/**
 * Onboarding Layout
 *
 * Minimal layout for the onboarding flow (payment → intake → build).
 * No sidebar, no navigation — just the flow.
 */

export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
